from chat_thief.new_models import SoundeffectRequest


class ApproveAllRequests:
    @staticmethod
    def approve(user):

        # Here we need to go through all Requests
        # for a user
        # and approve them
